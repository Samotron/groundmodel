[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "groundmodel"
version = "0.1.0"
description = "Python bindings for the groundmodel Rust core"
requires-python = ">=3.10"

[tool.maturin]
manifest-path = "Cargo.toml"
